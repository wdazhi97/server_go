# Leaderboard：单独构建时用 earthly ./leaderboard+docker（在仓库根目录执行）
VERSION 0.8

build:
  BUILD ../+leaderboard-build

docker:
  BUILD +build
  FROM ../buildtool+runtime
  COPY +build/leaderboard_server ./bin/leaderboard_server
  RUN chmod +x ./bin/*
  SAVE IMAGE snake-game-leaderboard:latest
