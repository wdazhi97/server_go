# Lobby：单独构建时用 earthly ./lobby+docker（在仓库根目录执行）
VERSION 0.8

build:
  BUILD ../+lobby-build

docker:
  BUILD +build
  FROM ../buildtool+runtime
  COPY +build/lobby_server ./bin/lobby_server
  RUN chmod +x ./bin/*
  SAVE IMAGE snake-game-lobby:latest
